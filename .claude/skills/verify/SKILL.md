---
name: verify
description: Type-check the project by running `npm run build`. Use after making changes to confirm the TypeScript compiles cleanly before committing.
---

Run `npm run build` from the project root. Report any TypeScript compiler errors. Success means tsc exits with code 0 and prints nothing.
